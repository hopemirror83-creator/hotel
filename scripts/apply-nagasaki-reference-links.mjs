import { readFile,writeFile } from 'node:fs/promises';
const path='src/data/generatedHotels.ts';const text=await readFile(path,'utf8');const match=text.match(/export const generatedHotels(?:\s*:\s*any\[\])?\s*=\s*([\s\S]*);\s*$/);const hotels=JSON.parse(match[1]);const collected=JSON.parse(await readFile('data/generated/hotels.collected.json','utf8'));const signals=new Map((collected.hotels||[]).map((h)=>[h.slug,h.sourceSignals||[]]));let linkedHotels=0,totalLinks=0;
for(const hotel of hotels){if(!hotel.slug?.startsWith('nagasaki-'))continue;const links=build(hotel.hotelName,signals.get(hotel.slug)||[]);if(links.length){hotel.referenceLinks=links;linkedHotels++;totalLinks+=links.length}else delete hotel.referenceLinks}await writeFile(path,`// @ts-nocheck\nexport const generatedHotels: any[] = ${JSON.stringify(hotels,null,2)};\n`);console.log({linkedHotels,totalLinks});
function build(name,sourceSignals){const seen=new Set(),links=[];for(const signal of sourceSignals)for(const item of signal.items||[]){const url=normalize(item.link),title=clean(item.title);if(!url||!title||seen.has(url)||!relevant(name,title))continue;seen.add(url);links.push({title,url,query:signal.query,source:'naver_blog'});if(links.length>=5)return links}return links}function relevant(name,title){const ignored=new Set(['호텔','리조트','료칸','나가사키','일본','숙소','후기','온천','더','앤','인']);const tokens=clean(name).toLowerCase().split(/[^0-9a-z가-힣]+/).filter((t)=>t.length>=2&&!ignored.has(t)),normalized=clean(title).toLowerCase().replace(/\s+/g,''),unique=[...new Set(tokens)];return unique.length>0&&unique.filter((t)=>normalized.includes(t)).length>=Math.min(2,unique.length)}function normalize(v){try{const url=new URL(String(v||''));if(!['blog.naver.com','m.blog.naver.com'].includes(url.hostname.toLowerCase()))return'';url.protocol='https:';url.hostname='blog.naver.com';return url.toString()}catch{return''}}function clean(v){return String(v||'').replace(/<[^>]+>/g,'').replace(/&quot;/g,'"').replace(/&amp;/g,'&').replace(/\s+/g,' ').trim()}
