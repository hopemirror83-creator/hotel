import{readFile,writeFile}from'node:fs/promises';const targets=new Set(JSON.parse(await readFile('data/target-slugs-yamaguchi-v1-quality.json','utf8'))),text=await readFile('src/data/generatedHotels.ts','utf8'),match=text.match(/export const generatedHotels(?:\s*:\s*any\[\])?\s*=\s*([\s\S]*);\s*$/),hotels=JSON.parse(match[1]).filter(h=>targets.has(h.slug)),failures=[],imageShortfalls=[],retrySlugs=[];for(const h of hotels){const sections=h.analysis?.blogReview?.sections||[],images=sections.filter(s=>/^https?:\/\//i.test(String(s.image?.url||s.imageUrl||''))).length,bodyLength=sections.reduce((n,s)=>n+(s.paragraphs||[]).reduce((m,p)=>m+String(p).length,0),0);if(sections.length<6)failures.push({slug:h.slug,issue:`sections:${sections.length}`});if(images<6)imageShortfalls.push({slug:h.slug,images});if(images<3)failures.push({slug:h.slug,issue:`images:${images}`});if(bodyLength<700)failures.push({slug:h.slug,issue:`body:${bodyLength}`});if(!/^https?:\/\//i.test(String(h.imageUrl||'')))failures.push({slug:h.slug,issue:'hero'});if(/Fukuoka|Oita|Shimane|Hiroshima|福岡|大分|島根|広島|후쿠오카|오이타|시마네|히로시마/i.test(String(h.address||'')))failures.push({slug:h.slug,issue:'region_outlier'});if(h.qualityStatus==='review_required'||bodyLength<700||sections.length<6)retrySlugs.push(h.slug)}await writeFile('data/target-hotels-yamaguchi-v1-generated.json',`${JSON.stringify(hotels,null,2)}\n`);await writeFile('data/target-slugs-yamaguchi-v1-retry.json',`${JSON.stringify([...new Set(retrySlugs)],null,2)}\n`);const summary={count:hotels.length,withSixSections:hotels.filter(h=>(h.analysis?.blogReview?.sections||[]).length>=6).length,withSixImages:hotels.filter(h=>(h.analysis?.blogReview?.sections||[]).filter(s=>/^https?:\/\//i.test(String(s.image?.url||s.imageUrl||''))).length>=6).length,withReferenceLinks:hotels.filter(h=>(h.referenceLinks||[]).length>0).length,retrySlugs:[...new Set(retrySlugs)],imageShortfalls,failures};console.log(JSON.stringify(summary,null,2));if(failures.length)process.exitCode=1;
