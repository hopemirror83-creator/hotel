import{readFile,writeFile}from'node:fs/promises';const candidates=JSON.parse(await readFile('data/candidates-akita-v1-all.json','utf8'));const manifest=JSON.parse(await readFile('deploy-data/generated-hotels/manifest.json','utf8'));const existing=[];for(const item of manifest.files)existing.push(...JSON.parse(await readFile(`deploy-data/generated-hotels/${item.filename}`,'utf8')));const ids=new Set(existing.map((h)=>Number(String(h.slug).split('-').at(-1))).filter(Number.isFinite));const selected=candidates.filter((h)=>!/Aomori|Iwate|Miyagi|Yamagata|Niigata|Appi|Hachimantai-shi|青森|岩手|宮城|山形|新潟|安比高原|아오모리|이와테|미야기|야마가타|니가타|앗피/i.test(`${h.hotelName} ${h.fallbackAddress||''}`)&&/^https?:\/\//i.test(String(h.imageUrl||''))&&Number(h.reviewCount||0)>=20&&Number(h.reviewScore||0)>=7.5&&!ids.has(Number(h.agodaHotelId))).slice(0,200).map((h)=>({...h,country:'일본',skipMapMatch:true}));await writeFile('data/target-hotels-akita-v1-quality.json',`${JSON.stringify(selected,null,2)}\n`);await writeFile('data/target-slugs-akita-v1-quality.json',`${JSON.stringify(selected.map((h)=>h.slug),null,2)}\n`);console.log(`Selected ${selected.length} strict Akita candidates from ${candidates.length} ranked candidates`);if(selected.length)console.log(`Minimum reviews: ${Math.min(...selected.map((h)=>Number(h.reviewCount||0)))}, minimum score: ${Math.min(...selected.map((h)=>Number(h.reviewScore||0)))}`);
