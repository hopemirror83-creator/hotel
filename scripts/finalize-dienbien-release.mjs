import{readFile,writeFile}from'node:fs/promises';const modulePath='src/data/generatedHotels.ts',source=await readFile(modulePath,'utf8'),match=source.match(/export const generatedHotels(?:\s*:\s*any\[\])?\s*=\s*([\s\S]*);\s*$/);if(!match)throw new Error('Unable to parse generatedHotels.ts');const hotels=JSON.parse(match[1]),kept=[],excluded=[];for(const hotel of hotels.filter(h=>h.slug?.startsWith('dienbien-'))){const sections=hotel.analysis?.blogReview?.sections||[],images=sections.filter(s=>/^https?:\/\//i.test(String(s.image?.url||s.imageUrl||''))).length,body=sections.reduce((t,s)=>t+(s.paragraphs||[]).reduce((a,p)=>a+String(p).length,0),0);if(hotel.qualityStatus!=='ready'||sections.length<6||images<6||body<700){excluded.push({slug:hotel.slug,status:hotel.qualityStatus,sections:sections.length,images,body});continue}kept.push(hotel)}await writeFile(modulePath,`// @ts-nocheck\nexport const generatedHotels: any[] = ${JSON.stringify(kept,null,2)};\n`);await writeFile('data/target-hotels-dienbien-v1-generated.json',`${JSON.stringify(kept,null,2)}\n`);await writeFile('data/target-slugs-dienbien-v1-quality.json',`${JSON.stringify(kept.map(h=>h.slug),null,2)}\n`);console.log({generated:hotels.length,kept:kept.length,excluded});
