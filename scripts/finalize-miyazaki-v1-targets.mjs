import{readFile,writeFile}from'node:fs/promises';const candidates=JSON.parse(await readFile('data/candidates-miyazaki-v1-all.json','utf8')),manifest=JSON.parse(await readFile('deploy-data/generated-hotels/manifest.json','utf8')),existing=[];for(const f of manifest.files)existing.push(...JSON.parse(await readFile(`deploy-data/generated-hotels/${f.filename}`,'utf8')));const ids=new Set(existing.map(h=>+String(h.slug).split('-').at(-1)).filter(Number.isFinite));const selected=candidates.filter(h=>!/Kagoshima|Kumamoto|Oita|鹿児島|熊本|大分|가고시마|구마모토|오이타/i.test(h.fallbackAddress||'')).filter(h=>/^https?:\/\//i.test(h.imageUrl||'')).filter(h=>+h.reviewCount>=20&&+h.reviewScore>=7.5&&!ids.has(+h.agodaHotelId)).slice(0,200).map(h=>({...h,country:'일본',skipMapMatch:true}));await writeFile('data/target-hotels-miyazaki-v1-quality.json',`${JSON.stringify(selected,null,2)}\n`);await writeFile('data/target-slugs-miyazaki-v1-quality.json',`${JSON.stringify(selected.map(h=>h.slug),null,2)}\n`);console.log(`Selected ${selected.length} strict Miyazaki candidates from ${candidates.length} ranked candidates`);if(selected.length)console.log(`Minimum reviews: ${Math.min(...selected.map(h=>+h.reviewCount))}, minimum score: ${Math.min(...selected.map(h=>+h.reviewScore))}`);
