import { readFile,writeFile } from 'node:fs/promises';const candidates=JSON.parse(await readFile('data/candidates-northernloop-v1-all.json','utf8')),manifest=JSON.parse(await readFile('deploy-data/generated-hotels/manifest.json','utf8')),existing=[];for(const file of manifest.files)existing.push(...JSON.parse(await readFile(`deploy-data/generated-hotels/${file.filename}`,'utf8')));const ids=new Set(existing.map(h=>Number(String(h.slug).split('-').at(-1))).filter(Number.isFinite)),names=new Set(),outlier=/Sapa|Sa Pa|사파|Lao Cai|Lào Cai|라오카이|Hanoi|Hà Nội|하노이|Lang Son|Lạng Sơn|랑선/i;const selected=candidates.filter(h=>!outlier.test(`${h.hotelName} ${h.fallbackAddress||''}`)).filter(h=>/^https?:\/\//i.test(h.imageUrl||'')).filter(h=>Number(h.reviewCount)>=20&&Number(h.reviewScore)>=7.5&&!ids.has(Number(h.agodaHotelId))).filter(h=>{const k=norm(h.hotelName);if(!k||names.has(k))return false;names.add(k);return true}).slice(0,200).map(h=>({...h,country:'베트남',skipMapMatch:true}));await writeFile('data/target-hotels-northernloop-v1-quality.json',`${JSON.stringify(selected,null,2)}\n`);await writeFile('data/target-slugs-northernloop-v1-quality.json',`${JSON.stringify(selected.map(h=>h.slug),null,2)}\n`);console.log(`Selected ${selected.length} strict Northern Loop candidates from ${candidates.length}`);if(selected.length)console.log(`Minimum reviews: ${Math.min(...selected.map(h=>Number(h.reviewCount)))}, minimum score: ${Math.min(...selected.map(h=>Number(h.reviewScore)))}`);function norm(v){return String(v||'').toLowerCase().replace(/\([^)]*\)|\[[^\]]*\]/g,'').replace(/[^a-z0-9가-힣]/g,'')}
