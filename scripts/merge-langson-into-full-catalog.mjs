import{execFileSync}from'node:child_process';import{readFile,writeFile}from'node:fs/promises';const show=f=>execFileSync('git',['show',`HEAD:${f}`],{encoding:'utf8',maxBuffer:1024*1024*300}),manifest=JSON.parse(show('deploy-data/generated-hotels/manifest.json')),existing=manifest.files.flatMap(f=>JSON.parse(show(`deploy-data/generated-hotels/${f.filename}`))).filter(h=>!h.slug?.startsWith('langson-')),region=JSON.parse(await readFile('data/target-hotels-langson-v1-generated.json','utf8')),merged=[...existing,...region];await writeFile('src/data/generatedHotels.ts',`// @ts-nocheck\nexport const generatedHotels: any[] = ${JSON.stringify(merged,null,2)};\n`);console.log({existing:existing.length,langson:region.length,total:merged.length});
