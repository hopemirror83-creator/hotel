import { execFileSync } from 'node:child_process';import { readFile,writeFile } from 'node:fs/promises';const show=(f)=>execFileSync('git',['show',`HEAD:${f}`],{encoding:'utf8',maxBuffer:1024*1024*300}),m=JSON.parse(show('deploy-data/generated-hotels/manifest.json')),existing=m.files.flatMap((f)=>JSON.parse(show(`deploy-data/generated-hotels/${f.filename}`))).filter((h)=>!h.slug?.startsWith('miyazaki-')),region=JSON.parse(await readFile('data/target-hotels-miyazaki-v1-generated.json','utf8')),merged=[...existing,...region];await writeFile('src/data/generatedHotels.ts',`// @ts-nocheck\nexport const generatedHotels: any[] = ${JSON.stringify(merged,null,2)};\n`);console.log({existing:existing.length,miyazaki:region.length,total:merged.length});
