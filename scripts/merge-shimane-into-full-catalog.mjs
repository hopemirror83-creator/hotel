import{execFileSync}from'node:child_process';import{readFile,writeFile}from'node:fs/promises';const show=p=>execFileSync('git',['show',`HEAD:${p}`],{encoding:'utf8',maxBuffer:1024*1024*300}),manifest=JSON.parse(show('deploy-data/generated-hotels/manifest.json')),head=manifest.files.flatMap(f=>JSON.parse(show(`deploy-data/generated-hotels/${f.filename}`))),existing=head.filter(h=>!h.slug?.startsWith('shimane-')),region=JSON.parse(await readFile('data/target-hotels-shimane-v1-generated.json','utf8')),merged=[...existing,...region];await writeFile('src/data/generatedHotels.ts',`// @ts-nocheck\nexport const generatedHotels: any[] = ${JSON.stringify(merged,null,2)};\n`);console.log({existing:existing.length,shimane:region.length,total:merged.length});
