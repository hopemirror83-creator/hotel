import{createReadStream}from'node:fs';import{writeFile}from'node:fs/promises';import{createInterface}from'node:readline';const csv=process.env.AGODA_CSV_PATH||'56A3C1A2-0531-49F3-8720-D7D4B1410E41_KO.csv',rows=createInterface({input:createReadStream(csv,{encoding:'utf8'}),crlfDelay:Infinity});let headers=null;const candidates=[];for await(const line of rows){if(!headers){headers=parse(line).map(v=>v.replace(/^\uFEFF/,''));continue}const values=parse(line);if(values.length<headers.length)continue;const row=Object.fromEntries(headers.map((h,i)=>[h,values[i]||'']));if(row.country!=='베트남')continue;const latitude=Number(row.latitude)||0,longitude=Number(row.longitude)||0,address=clean([row.addressline1,row.addressline2,row.city,row.state].filter(Boolean).join(' '));if(latitude<10.65||latitude>11.55||longitude<106.3||longitude>107.0||!isRegion(address)||isOutlier(address))continue;const id=Number(row.hotel_id),name=clean(row.hotel_translated_name||row.hotel_name);if(!Number.isInteger(id)||id<=0||!name||isOutlier(name))continue;candidates.push({slug:`binhduong-${id}`,hotelName:name,region:'베트남 빈즈엉',searchName:name,naverName:name,agodaHotelId:id,fallbackAddress:address,latitude,longitude,starRating:Number(row.star_rating)||undefined,reviewScore:Number(row.rating_average)||0,reviewCount:Number(row.number_of_reviews)||0,imageUrl:clean(row.photo1),landingUrl:`https://www.agoda.com/partners/partnersearch.aspx?pcs=1&cid=1927566&hid=${id}`})}candidates.sort((a,b)=>(b.reviewCount*b.reviewScore)-(a.reviewCount*a.reviewScore));await writeFile('data/candidates-binhduong-v1-all.json',JSON.stringify(candidates.slice(0,3000),null,2)+'\n');console.log(`Selected ${Math.min(3000,candidates.length)} Binh Duong candidates from ${candidates.length} matches`);function clean(v){return String(v||'').replace(/\s+/g,' ').trim()}function isRegion(v){return/Binh Duong|Bình Dương|빈즈엉|Thu Dau Mot|Thủ Dầu Một|투저우못|Thuan An|Thuận An|투언안|Di An|Dĩ An|디안|Ben Cat|Bến Cát|벤깟|Tan Uyen|Tân Uyên|Bau Bang|Bàu Bàng/i.test(v)}function isOutlier(v){return/Ho Chi Minh|Hồ Chí Minh|호치민|Bien Hoa|Biên Hòa|비엔호아|Dong Nai|Đồng Nai|동나이|Tay Ninh|Tây Ninh|떠이닌|Cu Chi|Củ Chi|꾸찌|Long An|롱안/i.test(v)}function parse(line){const r=[];let c='',q=false;for(let i=0;i<line.length;i++){const x=line[i],n=line[i+1];if(x==='"'&&q&&n==='"'){c+='"';i++}else if(x==='"')q=!q;else if(x===','&&!q){r.push(c);c=''}else c+=x}r.push(c);return r}
