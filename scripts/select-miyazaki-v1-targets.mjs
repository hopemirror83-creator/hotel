import{createReadStream}from'node:fs';import{writeFile}from'node:fs/promises';import{createInterface}from'node:readline';const csv=process.env.AGODA_CSV_PATH||'56A3C1A2-0531-49F3-8720-D7D4B1410E41_KO.csv',rows=createInterface({input:createReadStream(csv,{encoding:'utf8'}),crlfDelay:Infinity});let headers=null;const candidates=[];for await(const line of rows){if(!headers){headers=parse(line).map(v=>v.replace(/^\uFEFF/,''));continue}const values=parse(line);if(values.length<headers.length)continue;const row=Object.fromEntries(headers.map((h,i)=>[h,values[i]||'']));if(row.country!=='일본')continue;const lat=+row.latitude||0,lng=+row.longitude||0,address=clean([row.addressline1,row.addressline2,row.city,row.state].filter(Boolean).join(' '));if(lat<31.3||lat>32.9||lng<130.6||lng>132.05||!isRegion(address))continue;const id=+row.hotel_id,name=clean(row.hotel_translated_name||row.hotel_name);if(!Number.isInteger(id)||id<=0||!name)continue;candidates.push({slug:`miyazaki-${id}`,hotelName:name,region:'미야자키현',searchName:name,naverName:name,agodaHotelId:id,fallbackAddress:address,latitude:lat,longitude:lng,starRating:+row.star_rating||undefined,reviewScore:+row.rating_average||0,reviewCount:+row.number_of_reviews||0,imageUrl:clean(row.photo1),landingUrl:`https://www.agoda.com/partners/partnersearch.aspx?pcs=1&cid=1927566&hid=${id}`})}candidates.sort((a,b)=>(b.reviewCount*b.reviewScore)-(a.reviewCount*a.reviewScore));await writeFile('data/candidates-miyazaki-v1-all.json',`${JSON.stringify(candidates.slice(0,2000),null,2)}\n`);console.log(`Selected ${Math.min(2000,candidates.length)} Miyazaki candidates from ${candidates.length} coordinate and address matches`);function clean(v){return String(v||'').replace(/\s+/g,' ').trim()}function isRegion(v){return /Miyazaki Prefecture|宮崎県|미야자키현|\b(?:Miyazaki|Aoshima|Takachiho|Nobeoka|Hyuga|Nichinan|Miyakonojo|Ebino|Kobayashi|Saito|Kushima|Takaharu|Aya)\b|宮崎|青島|高千穂|延岡|日向|日南|都城|えびの|小林|西都|串間|高原|綾|미야자키|아오시마|다카치호|노베오카|휴가|니치난|미야코노조|에비노|고바야시|사이토|구시마/i.test(v)}function parse(line){const r=[];let c='',q=false;for(let i=0;i<line.length;i++){const x=line[i],n=line[i+1];if(x==='"'&&q&&n==='"'){c+='"';i++}else if(x==='"')q=!q;else if(x===','&&!q){r.push(c);c=''}else c+=x}r.push(c);return r}
